package Lambdas;

// anonimas, sao funcoes, tem o objetivo de deixar o codigo mais conciso
public class EntendendoLambdas {
    // para utilizar lambdas, é necessário uma interface funcional (apenas 1 metodo abstrato)
    //<parametro) -> <expressao>
    //(cars), car -> car.getColor.equalsIgnoreCase("green");
    // faz for each automaticamene, como se fosse "for (Car car: cars)"
}
