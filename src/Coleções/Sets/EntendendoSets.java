package Coleções.Sets;

public class EntendendoSets {
    // Set normal (HashSet) -> Se baseia no equals do objeto em questão para organizar o set
    // TreeSet/NavigableSet -> Se a classe do set em questão já tiver a Classe "Comparable" implementada nada precisa ser passado (organiza em base no metodo compareTo),
    // se não, pode ser utilizado passando um Comparator, ou uma Lambda. Além disso, ele utiliza do compareTo para comparar os elementos do set, portanto, os conceitos de igualdade são diferentes de HashSet que utiliza equals
    //
}
