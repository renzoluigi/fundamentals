package Coleções;

public class HashCode { //separa arrays em grandes blocos nomeados por indice, fazendo a pesquisa por indice, usando .equals em cada membro do bloco

    @Override
    public int hashCode() {
        return super.hashCode();
    }
}
